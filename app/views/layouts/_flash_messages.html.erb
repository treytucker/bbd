<% if flash %>
	<div id="flash-notice-container">
		<% flash.each do |name, msg| %>
		  	<%= content_tag :div, msg, :id => "flash_#{name}", :class => "flash-notice-box" %>
			<a id="flash-notice-closer" href="#">×</a>
		<% end %>
	</div>
<% end %>